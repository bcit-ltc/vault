// modules/policies/templates/kv_read.hcl.tmpl
# [RO] ${mount}
%{ for p in subpaths ~}
path "${mount}/metadata/${p}"   { capabilities = ["list"] }
path "${mount}/data/${p}"     { capabilities = ["read"] }
%{ endfor ~}
